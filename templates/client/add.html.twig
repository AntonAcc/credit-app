{% extends 'base.html.twig' %}

{% block title %}Add New Client{% endblock %}

{% block body %}
    <h1>Add New Client</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('client_list') }}">Back to Client List</a>
{% endblock %}
